node_modules/

# Tool-owned working directories, not repository content. Keeping them out of a
# clone is what makes the Load unpacked claim hold for someone who is not the
# maintainer.
_bmad/
_bmad-output/
